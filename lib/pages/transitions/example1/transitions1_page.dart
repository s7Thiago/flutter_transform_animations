import 'package:flutter/material.dart';

class Transitions1Page extends StatelessWidget {
  const Transitions1Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Custom Transitions'),
      ),
      body: Container(),
    );
  }
}
